Generics in haskell:

```haskell
-- Automatically inferred id :: a -> a
{{#include generics.hs}}
```

OCaml:

```ocaml
(* Automatically inferred val id : 'a -> 'a  *)

{{#include generics.ml}}
```

TypeScript:

```ts
{{#include generics.ts}}
```

Rust:

```rs

{{#include generics.rs}}
```

Python:

```py
{{#include generics.py}}
```
